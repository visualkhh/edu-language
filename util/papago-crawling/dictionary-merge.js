const fetch = require('node-fetch');
var qs = require('querystring');
const fs = require('fs');
// import utf8 from 'utf8';
const dictionary = require('../../english/dictionary')

console.log(JSON.stringify(dictionary.dictionary));
